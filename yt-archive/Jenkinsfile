pipeline {
    environment{
        AMD64BUILD=''
        ARM64BUILD=''
    }
    agent any
    stages {
        stage('Parallel builds'){
            parallel {
                stage('Start AMD64 build'){
                    steps{
                        script{
                            def res = build 'yt-archive-amd64'
                            echo "AMD64 build finished"
                            AMD64BUILD=res.number
                        }
                    }
                    post{
                        failure{
                            echo "AMD64 build failed"
                        }
                    }
                }
                stage('Start ARM64 build'){
                    steps{
                        script {
                            def res = build 'yt-archive-aarch64'
                            echo "ARM64 build finished"
                            ARM64BUILD=res.number
                        }
                    }
                    post{
                        failure{
                            echo "ARM64 build failed"
                        }
                    }
                }
            }
        }

        stage('Update manifest'){
            steps{
                script{
                    echo "AMD64BUILD: ${AMD64BUILD}"
                    echo "ARM64BUILD: ${ARM64BUILD}"
                    withCredentials([usernamePassword( credentialsId: 'DockerhubCredentials', usernameVariable: 'USER', passwordVariable: 'PASSWORD')]) {
                        sh "echo $PASSWORD | docker login -u $USER --password-stdin"
                        sh "docker pull tetricz/yt-archive:amd64-build-${AMD64BUILD} && docker pull tetricz/yt-archive:aarch64-build-${ARM64BUILD}"
                        sh "docker manifest create tetricz/yt-archive:latest tetricz/yt-archive:amd64-build-${AMD64BUILD} tetricz/yt-archive:aarch64-build-${ARM64BUILD}"
                        sh "docker manifest push tetricz/yt-archive:latest"
                    }
                }
            }
            post{
                always{
                    sh "docker manifest rm tetricz/yt-archive:latest"
                    sh "docker logout"
                }
            }
        }
    }
    post {
		always {
            sh 'docker system prune -fa'
			sh 'docker logout'
		}
	}
}
