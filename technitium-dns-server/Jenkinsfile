pipeline {
    environment{
        AMD64BUILD=''
        ARM64BUILD=''
    }
    agent any
    stages {
        stage('Parallel builds'){
            parallel {
                stage('Start AMD64 build'){
                    steps{
                        script{
                            def res = build 'technitium-dns-amd64'
                            echo "AMD64 build finished"
                            AMD64BUILD=res.number
                        }
                    }
                    post{
                        failure{
                            echo "AMD64 build failed"
                        }
                    }
                }
                stage('Start ARM64 build'){
                    steps{
                        script {
                            def res = build 'technitium-dns-aarch64'
                            echo "ARM64 build finished"
                            ARM64BUILD=res.number
                        }
                    }
                    post{
                        failure{
                            echo "ARM64 build failed"
                        }
                    }
                }
            }
        }

        stage('Update manifest'){
            steps{
                script{
                    echo "AMD64BUILD: ${AMD64BUILD}"
                    echo "ARM64BUILD: ${ARM64BUILD}"
                    withCredentials([usernamePassword( credentialsId: 'DockerhubCredentials', usernameVariable: 'USER', passwordVariable: 'PASSWORD')]) {
                        sh "echo $PASSWORD | docker login -u $USER --password-stdin"
                        sh "docker pull tetricz/technitium-dns:amd64-build-${AMD64BUILD} && docker pull tetricz/technitium-dns:aarch64-build-${ARM64BUILD}"
                        sh "docker manifest inspect tetricz/technitium-dns:latest"
                        sh "docker manifest create tetricz/technitium-dns:latest tetricz/technitium-dns:amd64-build-${AMD64BUILD} tetricz/technitium-dns:aarch64-build-${ARM64BUILD}"
                        sh "docker manifest push tetricz/technitium-dns:latest"
                        sh "docker manifest inspect tetricz/technitium-dns:latest"
                    }
                }
            }
            post{
                always{
                    sh "docker manifest rm tetricz/technitium-dns:latest"
                    sh "docker logout"
                }
            }
        }
    }
    post {
		always {
            sh 'docker system prune -fa'
			sh 'docker logout'
		}
	}
}
